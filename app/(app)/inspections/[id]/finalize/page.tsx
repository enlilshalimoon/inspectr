import { Card, CardContent } from "@/components/ui/card";

export default function FinalizePage() {
  return (
    <Card>
      <CardContent className="p-10 text-center space-y-2 text-sm text-slate-500">
        <p className="font-medium text-slate-700">Finalize not built yet</p>
        <p>Week 6 of the build: branded PDF export, client share link, email delivery.</p>
      </CardContent>
    </Card>
  );
}
