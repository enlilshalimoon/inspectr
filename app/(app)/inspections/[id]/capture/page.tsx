import { Card, CardContent } from "@/components/ui/card";

export default function CapturePage() {
  return (
    <Card>
      <CardContent className="p-10 text-center space-y-2 text-sm text-slate-500">
        <p className="font-medium text-slate-700">Mobile capture not built yet</p>
        <p>Week 2 of the build: take photos on a phone, voice-note per section, sync offline.</p>
      </CardContent>
    </Card>
  );
}
